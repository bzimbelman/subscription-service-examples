# 03-secure-channels environment.

# HMAC signing key for webhook deliveries. Generate with:
#   openssl rand -hex 32
# Keep this OUT of source control in real deployments — load from a Secret.
CHANNEL_HMAC_SECRET=replace-me-with-an-openssl-rand-hex-32-value
